import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  container: {},
  containerLabel: {},

  containerHour: {
    paddingBottom: 15,
  },
  label: {
    fontSize: 18,
  },
  labelHour: {
    fontSize: 14,
    color: '#ccc',
  },
});

export default styles;
