import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  container: {
    height: 200,
    backgroundColor: '#ddd',
  },

  image: {
    width: '100%',
    height: '100%',
  },
});

export default styles;
