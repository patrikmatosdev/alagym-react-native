import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  container: {
    padding: 15,
    flexDirection: 'row',
  },

  boxDetails: {
    flex: 1,
    alignItems: 'center',
  },

  title: {
    fontSize: 18,
    fontWeight: 'bold',
    marginTop: 10,
    marginBottom: 5,
  },
  label: {
    fontSize: 14,
    color: '#BABABA',
  },
});

export default styles;
