import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  container: {
    flexGrow: 0,
    flexShrink: 0,
    flexBasis: 200,
  },

  image: {
    width: '100%',
    height: '100%',
  },
});

export default styles;
