import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  containerTitle: {
    paddingBottom: 20,
  },

  title: {
    fontSize: 36,
    fontWeight: 'bold',
    fontFamily: 'Roboto',
  },
});

export default styles;
