import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  container: {
    paddingBottom: 15,
  },

  title: {
    color: '#191970',
    fontSize: 16,
  },
});

export default styles;
