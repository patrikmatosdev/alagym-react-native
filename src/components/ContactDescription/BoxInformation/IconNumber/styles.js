import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  container: {
    borderTopColor: '#ccc',
    borderTopWidth: 0.5,
    paddingTop: 10,
  },

  number: {
    fontSize: 18,
  },

  labelType: {
    color: '#ccc',
  },
});

export default styles;
